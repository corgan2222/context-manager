[package]
name = "ctxmenu"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Windows Context Menu Manager"

[dependencies]
eframe.workspace = true
egui.workspace = true
egui_extras.workspace = true
raw-window-handle.workspace = true
windows.workspace = true
windows-registry.workspace = true
serde.workspace = true
serde_json.workspace = true
anyhow.workspace = true
thiserror.workspace = true
chrono.workspace = true
dirs.workspace = true
rustc-hash.workspace = true

[build-dependencies]
winresource = "0.1.31"

[dev-dependencies]
# Reads a font's own character-to-glyph table, which is what decides whether a
# glyph is drawn or comes out as an empty box. Already in the tree below
# epaint, so this adds a line rather than a download.
read-fonts = "0.41"
