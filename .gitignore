# Build-Artefakte
/target
*.pdb

# Laufzeitdaten des Werkzeugs
/backups

# Enthält gültige API-Schlüssel (Kimi, DeepSeek, xAI) im Klartext.
# Darf niemals committet werden, auch nicht in einem privaten Repo.
.mcp.json

# Claude Code, lokal. Arbeitsnotizen, keine Projektdateien: sie bleiben auf
# der Platte und aus der Versionsverwaltung heraus. Am 2026-08-14 auch
# rückwirkend aus der Historie getilgt.
.claude/settings.local.json
CLAUDE.md
HANDOVER.md
handover_mcp2.md
# Am 2026-08-15 nach .claude/ gezogen: zurückgestellte Vorhaben sind eine
# Arbeitsnotiz wie die HANDOVER, keine ausgelieferte Dokumentation.
maybe_later.md
# Testbild fuer den Webtool-Durchlauf; liegt beim Benutzer, nicht im Repo.
test.png
