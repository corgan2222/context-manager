# Build artefacts. The released binary is built by GitHub from the tag and
# attached to the release there -- it never travels through a commit.
/target
/release-assets
*.exe
*.pdb

# The tool's runtime data
/backups

# The documentation site: dependencies and build output
website/node_modules/
website/.next/
website/out/
website/.source/
